# DOMAIN_NAME and SUBDOMAIN together determine where n8n will be reachable from
# The top level domain to serve from
DOMAIN_NAME=localhost

# The subdomain to serve from
SUBDOMAIN=

# The above example serve n8n at: https://n8n.example.com

# Optional timezone to set which gets used by Cron and other scheduling nodes
# The email address to use for the TLS/SSL certificate creation
SSL_EMAIL=carreira.outlier@gmail.com

# Host/URLs
GENERIC_TIMEZONE=America/Sao_Paulo
N8N_HOST=localhost
WEBHOOK_URL=http://localhost:5678/

# Postgres
POSTGRES_USER=dmenescal
POSTGRES_PASSWORD=C120915.d
POSTGRES_DB=n8n
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=n8n

# n8n
# N8N_ENCRYPTION_KEY=troque_isto_por_uma_chave_aleatoria_grande

# Permissões (use seu UID/GID do host)
PUID=1000
PGID=1000
